# Firmware Binaries

This folder is populated automatically by GitHub Actions.
When you push to main, CI builds all 4 firmwares and copies binaries here.
The ESP Web Tools flasher reads from the manifest.json files.